/*

package Practice;

public class recurSample {
    public void recur(int level, int params){
        // terminator
        if (level > max_level){
            // process
            return;
        }

        // process current logic
        process(level, data...);

        // drill down
        recur(level: level +1, int newParams);

        // recover current status if needed
    }
}

*/
